<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chrisvasqm.cuadramo">

    <application
        android:name=".CuadramoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.chrisvasqm.cuadramo.view.catalog.CatalogActivity"
            android:exported="true"
            android:theme="@style/SplashTheme"
            android:label="@string/catalog">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.chrisvasqm.cuadramo.view.editor.EditorActivity"
            android:label="@string/editor"
            android:parentActivityName="com.chrisvasqm.cuadramo.view.catalog.CatalogActivity" />
        <activity
            android:name="com.chrisvasqm.cuadramo.view.about.AboutActivity"
            android:label="@string/about"
            android:parentActivityName="com.chrisvasqm.cuadramo.view.catalog.CatalogActivity" />
    </application>

</manifest>